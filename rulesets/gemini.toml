# GVM Ruleset: Gemini
# Covers: gemini skill
# Domains: generativelanguage.googleapis.com

[[rules]]
pattern = "generativelanguage.googleapis.com/v1beta/models/*"
method = "POST"
decision = "Allow"
reason = "Gemini inference (cost tracked via WAL)"

[[rules]]
pattern = "generativelanguage.googleapis.com/v1beta/models"
method = "GET"
decision = "Allow"
reason = "List models (read-only)"

[[rules]]
pattern = "generativelanguage.googleapis.com/v1/models/*"
method = "POST"
decision = "Allow"
reason = "Gemini inference (cost tracked via WAL)"

[[rules]]
pattern = "generativelanguage.googleapis.com/v1/models"
method = "GET"
decision = "Allow"
reason = "List models (read-only)"
